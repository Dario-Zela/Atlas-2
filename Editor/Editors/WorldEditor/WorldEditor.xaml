<UserControl x:Class="Editor.Editors.WorldEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.Editors" xmlns:editor="clr-namespace:Editor"
             mc:Ignorable="d" KeyDown="UserControl_KeyDown" Focusable="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
            <TextBlock x:Name="Test"/>
            <Button IsEnabled="{Binding Path=(editor:UndoRedoManager.IsUndoAvailable)}" Click="Undo_ButtonClick" Margin="0,0,5,0">Undo</Button>
            <Button IsEnabled="{Binding Path=(editor:UndoRedoManager.IsRedoAvailable)}" Click="Redo_ButtonClick" Margin="0,0,5,0">Redo</Button>
            <Button Click="Save_ButtonClick">Save</Button>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <local:HierarchyView/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
