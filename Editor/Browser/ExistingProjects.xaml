<UserControl x:Class="Editor.Browser.Existing_Projects"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.Browser"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="32">
            Projects
        </TextBlock>
        <Button Width="auto" FontSize="32" Margin="0,0,30,0" HorizontalAlignment="Right">
            Add
        </Button>
        <ListView Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock/>
                        <TextBlock/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListViewItem FontSize="32">Test</ListViewItem>
            <ListViewItem FontSize="32">Test</ListViewItem>
            <ListViewItem FontSize="32">Test</ListViewItem>
            <ListViewItem FontSize="32">Test</ListViewItem>
        </ListView>
    </Grid>
</UserControl>
